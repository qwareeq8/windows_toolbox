---
phase: 04-snap-and-explorer-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - virelo/services/snap.py
autonomous: true
requirements:
  - SNAP-01
  - SNAP-02
  - SNAP-03
  - SNAP-04

must_haves:
  truths:
    - "HotkeyListener class owns all keyboard hook management, press timing, and trigger logic"
    - "ShiftSnapRestore class retains only window movement logic (perform, _snap, _restore)"
    - "SnapService delegates update_binding/update_restore_key/update_press_limit to HotkeyListener via set_listener"
    - "Snapping while Virelo's own window is focused does nothing (no center, no move)"
    - "Restoring while Virelo's own window is focused does nothing"
    - "Debug log emitted when Virelo's window is skipped in both _snap and _restore"
  artifacts:
    - path: "virelo/services/snap.py"
      provides: "HotkeyListener, ShiftSnapRestore, SnapService, calculate_snap_position"
      contains: "class HotkeyListener"
  key_links:
    - from: "HotkeyListener"
      to: "ShiftSnapRestore.perform"
      via: "triggered(bool) signal"
      pattern: "triggered = QtCore\\.Signal\\(bool\\)"
    - from: "SnapService"
      to: "HotkeyListener"
      via: "set_listener method"
      pattern: "def set_listener"
---

<objective>
Split ShiftSnapRestore into HotkeyListener (keyboard detection) and ShiftSnapRestore (window movement), update SnapService delegation, and implement Virelo window exclusion.

Purpose: Separates keyboard hook management from window manipulation for single responsibility and testability. Fixes the unexpected behavior of centering Virelo's own window when snap is triggered while Virelo is focused.

Output: Modified virelo/services/snap.py with three classes (HotkeyListener, ShiftSnapRestore, SnapService) and calculate_snap_position pure function.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-snap-and-explorer-hardening/04-CONTEXT.md
@.planning/phases/04-snap-and-explorer-hardening/04-RESEARCH.md
@.planning/phases/04-snap-and-explorer-hardening/04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From virelo/services/snap.py (current SnapService API -- must be preserved):
```python
class SnapService:
    def __init__(self, shift_mgr): ...
    def set_manager(self, mgr): ...
    def test_snap(self) -> dict: ...
    def update_binding(self, key: str): ...
    def update_restore_key(self, key: str): ...
    def update_press_limit(self, count: int): ...
```

From virelo/services/snap.py (current ShiftSnapRestore -- signals):
```python
class ShiftSnapRestore(QtCore.QObject):
    triggered = QtCore.Signal(bool)  # MOVES to HotkeyListener
    blocked = QtCore.Signal(str)     # STAYS on ShiftSnapRestore
```

From virelo/app/config.py:
```python
def normalize_snap_presses(value) -> int: ...
```

From virelo/bridge/bridge.py (consumers of SnapService -- must NOT break):
```python
# Line 273: mw.shift_mgr.update_press_limit(applied["snap_presses"])
# Line 276: mw.shift_mgr.update_binding(applied["snap_key"])
# Line 279: mw.shift_mgr.update_restore_key(applied["restore_key"])
```
Note: bridge.py calls these on mw.shift_mgr directly. After Plan 02 wires
the HotkeyListener in window.py, bridge.py will be updated to delegate
through the appropriate object. For this plan, SnapService gains set_listener
so Plan 02 can rewire cleanly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HotkeyListener from ShiftSnapRestore and update SnapService</name>
  <files>virelo/services/snap.py</files>
  <read_first>
    - virelo/services/snap.py (full file -- current ShiftSnapRestore class to split)
    - virelo/app/config.py (normalize_snap_presses used by both classes)
    - .planning/phases/04-snap-and-explorer-hardening/04-PATTERNS.md (exact code blocks to extract)
  </read_first>
  <action>
Rewrite virelo/services/snap.py with three classes plus the existing calculate_snap_position function. The file structure top-to-bottom:

1. **Imports** -- unchanged from current file (lines 1-31). No new imports needed.

2. **calculate_snap_position** function -- unchanged (lines 34-47).

3. **class HotkeyListener(QtCore.QObject)** -- NEW class. Extract these members verbatim from current ShiftSnapRestore:
   - `triggered = QtCore.Signal(bool)` -- the signal MOVES here from ShiftSnapRestore
   - `__init__(self, settings)` -- takes `settings`, initializes: `self.settings = settings`, `self._press_times = deque(maxlen=normalize_snap_presses(self.settings.snap_presses))`, `self._press_lock = threading.Lock()`, `self._held = False`, `self.current_key = str(settings.snap_key)`, `self.restore_key = str(getattr(settings, "restore_key", "ctrl"))`, `self._press_hook = keyboard.on_press_key(self.current_key, self._on_press)`, `self._release_hook = keyboard.on_release_key(self.current_key, self._on_release)`. Call `super().__init__()` first.
   - `cleanup(self)` -- verbatim from current lines 112-120 (unhook press/release with try/except)
   - `update_binding(self, new_key: str)` -- verbatim from current lines 173-189
   - `update_restore_key(self, new_key: str)` -- verbatim from current lines 191-192
   - `update_press_limit(self, new_limit: int)` -- verbatim from current lines 194-196
   - `_on_press(self, event)` -- verbatim from current lines 198-219. The `self.triggered.emit(restore)` call now emits from HotkeyListener.triggered (correct since the signal is on this class).
   - `_on_release(self, event)` -- verbatim from current lines 221-222
   - Docstring: `"""Detects multi-press keyboard patterns and emits trigger signal."""`

4. **class ShiftSnapRestore(QtCore.QObject)** -- MODIFIED class. Remove all keyboard logic. Keep:
   - `blocked = QtCore.Signal(str)` -- stays (NO triggered signal on this class anymore)
   - `__init__(self, settings)` -- `super().__init__()`, `self.settings = settings`, `self._orig_sizes: dict[int, dict[str, tuple[int, int, int, int] | bool]] = {}`, `self._fetch_open_windows()`. NO keyboard hooks, NO press_times, NO current_key, NO restore_key, NO _held, NO _press_lock.
   - `_fetch_open_windows(self)` -- unchanged from current lines 122-159
   - `_prune_closed_windows(self)` -- unchanged from current lines 161-171
   - `perform(self, restore: bool)` -- unchanged, keep @QtCore.Slot(bool) decorator (current lines 224-236)
   - `_snap(self, hwnd: int)` -- MODIFIED for Virelo window exclusion (see Task 2)
   - `_restore(self, hwnd: int)` -- MODIFIED for Virelo window exclusion (see Task 2)
   - Docstring: `"""Performs window snap and restore operations."""`

5. **class SnapService** -- MODIFIED. Add `_listener` attribute and `set_listener` method:
   - `__init__(self, shift_mgr)` -- add `self._listener = None` after `self._mgr = shift_mgr`
   - `set_manager(self, mgr)` -- unchanged
   - NEW `set_listener(self, listener)` -- `"""Set or replace the HotkeyListener instance."""` body: `self._listener = listener`
   - `test_snap(self) -> dict` -- unchanged (still delegates to self._mgr.perform)
   - `update_binding(self, key: str)` -- change body to: `if self._listener: self._listener.update_binding(key)`
   - `update_restore_key(self, key: str)` -- change body to: `if self._listener: self._listener.update_restore_key(key)`
   - `update_press_limit(self, count: int)` -- change body to: `if self._listener: self._listener.update_press_limit(count)`
   - Docstring: unchanged `"""Narrow API surface for snap/restore actions."""`

IMPORTANT: Do NOT remove the `keyboard`, `threading`, `time`, or `deque` imports -- HotkeyListener still uses them. Do NOT add any new imports.
  </action>
  <verify>
    <automated>cd D:/projects/Virelo && python -c "from virelo.services.snap import HotkeyListener, ShiftSnapRestore, SnapService, calculate_snap_position; print('All 4 symbols importable')"</automated>
  </verify>
  <acceptance_criteria>
    - virelo/services/snap.py contains `class HotkeyListener(QtCore.QObject):`
    - virelo/services/snap.py contains `class ShiftSnapRestore(QtCore.QObject):`
    - virelo/services/snap.py contains `class SnapService:`
    - HotkeyListener has `triggered = QtCore.Signal(bool)`
    - ShiftSnapRestore does NOT have `triggered = QtCore.Signal(bool)` (grep returns no match within ShiftSnapRestore class)
    - ShiftSnapRestore has `blocked = QtCore.Signal(str)`
    - ShiftSnapRestore.__init__ does NOT contain `keyboard.on_press_key` or `keyboard.on_release_key`
    - HotkeyListener.__init__ contains `keyboard.on_press_key(self.current_key, self._on_press)`
    - SnapService contains `def set_listener(self, listener):`
    - SnapService.update_binding body references `self._listener` not `self._mgr`
    - SnapService.update_restore_key body references `self._listener` not `self._mgr`
    - SnapService.update_press_limit body references `self._listener` not `self._mgr`
    - calculate_snap_position function unchanged at top of file
  </acceptance_criteria>
  <done>HotkeyListener class owns all keyboard hooks and timing logic. ShiftSnapRestore retains only window movement. SnapService delegates key binding methods to HotkeyListener via _listener. All four symbols importable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Virelo window exclusion in _snap and _restore</name>
  <files>virelo/services/snap.py</files>
  <read_first>
    - virelo/services/snap.py (as modified by Task 1 -- current state)
    - .planning/phases/04-snap-and-explorer-hardening/04-PATTERNS.md (exact replacement code for _snap and _restore Qt widget blocks)
  </read_first>
  <action>
Modify the _snap and _restore methods in ShiftSnapRestore to skip Virelo's own window entirely instead of centering it.

**In _snap(self, hwnd: int)** -- replace the Qt widget loop (the block that checks `QtWidgets.QApplication.topLevelWidgets()` and calls `center_on_screen()`) with:
```python
for widget in QtWidgets.QApplication.topLevelWidgets():
    if int(widget.winId()) == hwnd:
        LOG.debug("Snap: skipping Virelo's own window hwnd=%s", hwnd)
        return  # Skip entirely per SNAP-03
```
This removes the `center_fn = getattr(widget, "center_on_screen", None)` / `center_fn()` / `widget.raise_()` / `widget.activateWindow()` lines. Per D-04, when snap targets Virelo's own window, skip entirely with no action.

**In _restore(self, hwnd: int)** -- replace the Qt widget loop (the block that restores widget geometry) with:
```python
for widget in QtWidgets.QApplication.topLevelWidgets():
    if int(widget.winId()) == hwnd:
        LOG.debug("Restore: skipping Virelo's own window hwnd=%s", hwnd)
        return  # Skip entirely per D-05
```
This removes the `self._orig_sizes.pop(hwnd, None)` / `widget.setGeometry(...)` / `widget.raise_()` / `widget.activateWindow()` lines for Virelo's window. Per D-05, if we never snap it, there is nothing to restore.

Both methods retain their `from PySide6 import QtWidgets` local import at the top (unchanged).
  </action>
  <verify>
    <automated>cd D:/projects/Virelo && python -c "import ast; tree = ast.parse(open('virelo/services/snap.py').read()); methods = {node.name: ast.dump(node) for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}; snap_src = open('virelo/services/snap.py').read(); assert 'center_on_screen' not in snap_src, 'center_on_screen still present'; assert 'skipping Virelo' in snap_src, 'skip log missing'; print('Virelo exclusion verified')"</automated>
  </verify>
  <acceptance_criteria>
    - virelo/services/snap.py does NOT contain the string `center_on_screen`
    - virelo/services/snap.py contains `LOG.debug("Snap: skipping Virelo's own window hwnd=%s", hwnd)`
    - virelo/services/snap.py contains `LOG.debug("Restore: skipping Virelo's own window hwnd=%s", hwnd)`
    - _snap method still contains `from PySide6 import QtWidgets` local import
    - _restore method still contains `from PySide6 import QtWidgets` local import
    - _snap method still contains the game mode check (`_should_skip_snap_for_game`)
    - _restore method still contains `was_maximized` logic and `win32gui.ShowWindow(hwnd, win32con.SW_MAXIMIZE)`
    - pytest tests/unit/test_snap_geometry.py passes (existing geometry tests unaffected)
  </acceptance_criteria>
  <done>Snapping while Virelo's own window is focused logs a debug message and returns without any action. Restoring while Virelo is focused does the same. No centering, no geometry change, no raise/activate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard hook -> snap logic | OS-level keyboard events trigger window manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | HotkeyListener._on_press | accept | Keyboard hooks run with admin privileges; same trust level as before split. No new attack surface. |
| T-04-02 | Denial of Service | ShiftSnapRestore._snap | accept | Snap only affects foreground window; Virelo exclusion reduces blast radius. |
</threat_model>

<verification>
1. `python -c "from virelo.services.snap import HotkeyListener, ShiftSnapRestore, SnapService, calculate_snap_position"` succeeds
2. `grep -c "class HotkeyListener" virelo/services/snap.py` returns 1
3. `grep -c "class ShiftSnapRestore" virelo/services/snap.py` returns 1
4. `grep -c "center_on_screen" virelo/services/snap.py` returns 0
5. `grep -c "skipping Virelo" virelo/services/snap.py` returns 2 (once in _snap, once in _restore)
6. `pytest tests/unit/test_snap_geometry.py -x` passes
</verification>

<success_criteria>
- HotkeyListener owns keyboard hooks and emits triggered(bool) signal
- ShiftSnapRestore owns only window movement, receives trigger via signal connection
- SnapService delegates key binding methods to HotkeyListener
- Virelo's window is excluded from both snap and restore with debug logging
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-snap-and-explorer-hardening/04-01-SUMMARY.md`
</output>
