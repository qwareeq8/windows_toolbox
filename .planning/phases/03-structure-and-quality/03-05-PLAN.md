---
phase: 03-structure-and-quality
plan: 05
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - .github/workflows/ci.yml
  - CLAUDE.md
autonomous: true
requirements: [QUAL-05, QUAL-06]
must_haves:
  truths:
    - "CI workflow exists with four jobs: lint, test, frontend, stale-name"
    - "CI triggers on push and pull_request to main branch"
    - "CI lint job runs ruff check and ruff format --check"
    - "CI test job runs pytest tests/unit/ (not integration)"
    - "CI frontend job runs npm ci, npm build, and vitest"
    - "CI stale-name job greps for 'Windows Toolbox' and fails if found"
    - "CI caches pip and npm dependencies"
    - "CLAUDE.md project structure section is updated to reflect virelo/ package layout"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "ruff check ."
    - path: "CLAUDE.md"
      provides: "Updated project documentation"
      contains: "virelo/"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pip install ruff reads config"
      pattern: "ruff check"
    - from: ".github/workflows/ci.yml"
      to: "tests/unit/"
      via: "pytest tests/unit/"
      pattern: "pytest tests/unit"
    - from: ".github/workflows/ci.yml"
      to: "frontend/"
      via: "npm ci + npm build + vitest"
      pattern: "vitest run"
---

<objective>
Create the GitHub Actions CI workflow with four jobs (lint, test, frontend, stale-name) and update CLAUDE.md to reflect the new virelo/ package structure.

Purpose: Establishes continuous integration that catches regressions on every push/PR (QUAL-05, QUAL-06). Updates project documentation so Claude Code (and the developer) understands the new layout.
Output: .github/workflows/ci.yml, updated CLAUDE.md.
</objective>

<execution_context>
@.planning/phases/03-structure-and-quality/03-CONTEXT.md
@.planning/phases/03-structure-and-quality/03-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- CI workflow references these artifacts from previous plans -->

From pyproject.toml (Plan 01):
- [tool.ruff] configured with E, F, I, UP rules
- [tool.pytest.ini_options] with testpaths = ["tests"]
- [project.optional-dependencies] dev = ["ruff>=0.15.12", "pytest>=9.0.3", ...]

From tests/ (Plan 04):
- tests/unit/*.py -- pure logic tests, no PySide6/Qt dependency
- tests/integration/ -- skipped in CI

From frontend/ (Plans 01, 04):
- package.json has "test": "vitest run"
- vite.config.js has test block with jsdom
- src/__tests__/*.test.jsx -- Vitest test files

Current CLAUDE.md project structure:
```
- `main.py` -- Application entry point, MainWindow, ShiftSnapRestore
- `bridge.py` -- QWebChannel bridge (VireloBridge QObject)
- `webview.py` -- QWebEngineView host
- `settings.py` / `settings_state.py` -- Settings persistence via QSettings
- `app_config.py` -- Product metadata, defaults, version (single source of truth)
- `snap_service.py` -- Snap service facade
- `explorer_columns.py` -- COM-based Explorer column autosize
- `workers.py` -- Background QThread workers
- `theme.py` -- Theme resolution (system/dark/light)
- `capture_guard.py` -- Thread-safe key capture mutex
- `startup_shortcut.py` -- Startup shortcut management
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - CLAUDE.md (forbidden changes, naming conventions)
    - pyproject.toml (verify ruff and pytest config exist)
    - frontend/package.json (verify test script exists)
  </read_first>
  <action>
Create `.github/workflows/` directory structure and the CI workflow file.

Create `.github/workflows/ci.yml` with exactly this content:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install ruff
      - run: ruff check .
      - run: ruff format --check .

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install -e ".[dev]"
      - run: pytest tests/unit/ -q

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npm run build
        working-directory: frontend
      - run: npx vitest run
        working-directory: frontend

  stale-name:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for stale "Windows Toolbox" references
        run: |
          if grep -rn "Windows Toolbox" . \
            --include="*.py" --include="*.jsx" --include="*.js" \
            --include="*.json" --include="*.toml" --include="*.yml" \
            --include="*.iss" --include="*.spec" \
            --exclude-dir=node_modules --exclude-dir=.git \
            --exclude-dir=dist --exclude-dir=build \
            --exclude-dir=.planning; then
            echo "ERROR: Found stale 'Windows Toolbox' references"
            exit 1
          fi
          echo "OK: No stale references found"
```

Per D-14: Single workflow with four jobs (lint, test, frontend, stale-name).
Per D-15: Ubuntu runner for all jobs.
Per D-16: Triggers on push and pull_request to main branch.
Per D-17: pip and npm caching enabled via actions/setup-python and actions/setup-node cache parameters.
Per D-10: Only `tests/unit/` runs in CI (no admin-requiring integration tests).
Per QUAL-06: stale-name job greps for "Windows Toolbox" and fails the build if found.

Note: Added `--exclude-dir=.planning` to the stale-name grep to avoid false positives from planning documents that reference the old name in historical context.
  </action>
  <verify>
    <automated>python -c "
import yaml
with open('.github/workflows/ci.yml') as f:
    ci = yaml.safe_load(f)
jobs = list(ci['jobs'].keys())
assert 'lint' in jobs, 'missing lint job'
assert 'test' in jobs, 'missing test job'
assert 'frontend' in jobs, 'missing frontend job'
assert 'stale-name' in jobs, 'missing stale-name job'
assert ci['on']['push']['branches'] == ['main'], 'wrong push trigger'
assert ci['on']['pull_request']['branches'] == ['main'], 'wrong PR trigger'
print(f'CI OK: {len(jobs)} jobs on main branch')
" 2>&amp;1 || python -c "
# Fallback if pyyaml not installed
with open('.github/workflows/ci.yml') as f:
    content = f.read()
checks = [
    ('lint:', 'lint job'),
    ('test:', 'test job'),
    ('frontend:', 'frontend job'),
    ('stale-name:', 'stale-name job'),
    ('ruff check .', 'ruff check'),
    ('ruff format --check .', 'ruff format check'),
    ('pytest tests/unit/', 'pytest unit'),
    ('npx vitest run', 'vitest run'),
    ('Windows Toolbox', 'stale name grep'),
    ('branches: [main]', 'main branch trigger'),
    ('cache:', 'caching'),
]
for pattern, name in checks:
    assert pattern in content, f'Missing: {name}'
print(f'CI OK: all {len(checks)} patterns found')
"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists
    - ci.yml contains `name: CI`
    - ci.yml contains `push:` and `pull_request:` triggers with `branches: [main]`
    - ci.yml contains `lint:` job with `ruff check .` and `ruff format --check .`
    - ci.yml contains `test:` job with `pytest tests/unit/ -q`
    - ci.yml contains `frontend:` job with `npm ci`, `npm run build`, and `npx vitest run`
    - ci.yml contains `stale-name:` job with `grep -rn "Windows Toolbox"`
    - ci.yml contains `cache: 'pip'` in lint and test jobs
    - ci.yml contains `cache: 'npm'` in frontend job
    - ci.yml uses `ubuntu-latest` for all jobs
    - ci.yml uses `python-version: '3.12'`
    - ci.yml uses `node-version: '22'`
    - ci.yml test job runs `pip install -e ".[dev]"` (not just pytest)
    - ci.yml stale-name excludes `.planning` directory
  </acceptance_criteria>
  <done>GitHub Actions CI workflow with 4 jobs (lint, test, frontend, stale-name) on ubuntu-latest, triggered on push/PR to main, with pip and npm caching.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md project structure to reflect virelo/ package</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current content -- being updated)
    - virelo/ directory listing (verify actual structure after Plans 02-03)
  </read_first>
  <action>
Update the `## Project Structure` section of CLAUDE.md to reflect the new virelo/ package layout. Replace the old flat-module listing with:

```markdown
## Project Structure

- `main.py` -- Thin entry shim: `from virelo.app import main; main()`
- `virelo/` -- Python package (all backend source)
  - `app/` -- Application shell
    - `__main__.py` -- Startup: admin elevation, single-instance, QApp, MainWindow
    - `window.py` -- MainWindow: tray, chrome, thread lifecycle, wiring
    - `config.py` -- Product metadata, defaults, version (single source of truth)
    - `webview.py` -- QWebEngineView host
  - `bridge/` -- Python-JS communication
    - `bridge.py` -- QWebChannel bridge (VireloBridge QObject)
    - `capture_guard.py` -- Thread-safe key capture mutex
  - `services/` -- Business logic
    - `snap.py` -- Snap service facade, ShiftSnapRestore engine, geometry calculations
    - `explorer_columns.py` -- COM-based Explorer column autosize
    - `theme.py` -- Theme resolution (system/dark/light)
    - `startup.py` -- Startup shortcut management
  - `workers/` -- Background QThread workers
    - `key_capture.py` -- KeyCaptureWorker and KeyCaptureSession
    - `explorer.py` -- ExplorerAutosizeWorker and ExplorerAutosizeEngine
  - `platform/` -- Windows platform utilities
    - `win32_helpers.py` -- DPI, monitor rects, fullscreen detection, window geometry
    - `resources.py` -- PyInstaller-compatible resource path resolution
    - `paths.py` -- Path canonicalization
  - `settings/` -- Settings persistence
    - `persistence.py` -- QSettings read/write (Settings class)
    - `state.py` -- JSON facade with validation and draft model (SettingsState class)
- `frontend/src/` -- React 19 frontend (app.jsx, pages.jsx, panels.jsx, etc.)
- `tests/` -- Test suite
  - `unit/` -- Pure logic tests (no Qt, runs in CI)
  - `integration/` -- Tests requiring PySide6 (local only)
- `scripts/` -- PowerShell build pipeline
- `installer/virelo.iss` -- Inno Setup installer script
- `Virelo.spec` -- PyInstaller spec file
- `pyproject.toml` -- Project metadata, dependencies, Ruff and pytest config
- `.github/workflows/ci.yml` -- CI: lint, test, frontend build, stale-name check
```

Also update the `## Known Footguns` section, item 1:
Change `app_config.py` references to `virelo/app/config.py`:

```markdown
1. **`virelo/app/config.py` must not be imported in `Virelo.spec`.** The spec file runs in PyInstaller's analysis context where PySide6 may not be importable. Use regex to parse the version string instead of importing the module.
```

Update item 4 in Forbidden Changes to reflect new config location:

```markdown
4. **Never hardcode version strings.** Use `APP_VERSION` from `virelo/app/config.py`. The frontend receives the version via Vite `define` at build time (`__APP_VERSION__`). The installer receives it via ISCC `/D` flag.
```

Keep all other sections of CLAUDE.md unchanged (Build Commands, Dev Mode, Naming Conventions, other Forbidden Changes, other Known Footguns).
  </action>
  <verify>
    <automated>python -c "
with open('CLAUDE.md') as f:
    content = f.read()
checks = [
    ('virelo/', 'virelo package reference'),
    ('app/__main__.py', '__main__.py reference'),
    ('app/window.py', 'window.py reference'),
    ('bridge/bridge.py', 'bridge.py reference'),
    ('services/snap.py', 'snap.py reference'),
    ('platform/win32_helpers.py', 'win32_helpers reference'),
    ('settings/persistence.py', 'persistence.py reference'),
    ('tests/', 'tests directory'),
    ('pyproject.toml', 'pyproject.toml reference'),
    ('ci.yml', 'CI workflow reference'),
    ('virelo/app/config.py', 'updated config path in footguns'),
]
for pattern, name in checks:
    assert pattern in content, f'CLAUDE.md missing: {name}'
print(f'CLAUDE.md OK: all {len(checks)} patterns found')
"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Project Structure section lists `virelo/` package with subpackages
    - CLAUDE.md contains `main.py` described as "Thin entry shim"
    - CLAUDE.md contains `virelo/app/__main__.py` described as startup
    - CLAUDE.md contains `virelo/app/window.py` described as MainWindow
    - CLAUDE.md contains `virelo/app/config.py` described as metadata/defaults/version
    - CLAUDE.md contains `tests/` with `unit/` and `integration/` subdirectories
    - CLAUDE.md contains `pyproject.toml` reference
    - CLAUDE.md contains `.github/workflows/ci.yml` reference
    - CLAUDE.md Known Footguns references `virelo/app/config.py` (not `app_config.py`)
    - CLAUDE.md Forbidden Changes item 4 references `virelo/app/config.py` (not `app_config.py`)
    - CLAUDE.md does NOT contain references to old flat-module paths (bridge.py at root, workers.py at root, etc.) in the Project Structure section
  </acceptance_criteria>
  <done>CLAUDE.md updated with virelo/ package structure, test suite references, pyproject.toml, CI workflow. All file path references in footguns and forbidden changes updated to new locations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner | CI runs on ubuntu-latest with network access to PyPI and npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | CI supply chain | accept | Uses official GitHub Actions (actions/checkout@v4, actions/setup-python@v5, actions/setup-node@v4). Version-pinned. Standard practice for open-source projects. |
| T-03-10 | Denial of Service | CI resource consumption | accept | Four lightweight jobs on ubuntu-latest free tier. pip and npm caching reduces network calls. |
| T-03-11 | Information Disclosure | Stale name in planning docs | mitigate | stale-name grep excludes `.planning/` directory to avoid false positives from historical references while still catching actual source code regressions. |
</threat_model>

<verification>
1. .github/workflows/ci.yml exists with 4 jobs
2. CI triggers on push and pull_request to main
3. stale-name job excludes .planning directory
4. CLAUDE.md reflects the new virelo/ package structure
5. No references to old flat-module paths in CLAUDE.md Project Structure
</verification>

<success_criteria>
CI pipeline is ready to enforce quality on every push/PR: Ruff lint and format, pytest unit tests, Vitest frontend tests, and stale-name regression gate (QUAL-05, QUAL-06). CLAUDE.md accurately documents the restructured project.
</success_criteria>

<output>
After completion, create `.planning/phases/03-structure-and-quality/03-05-SUMMARY.md`
</output>
