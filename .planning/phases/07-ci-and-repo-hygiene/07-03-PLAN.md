---
phase: 07-ci-and-repo-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - frontend/package.json
  - README.md
  - installer/virelo.iss
autonomous: true
requirements:
  - CI-05
  - CI-06
  - CI-07

must_haves:
  truths:
    - "frontend/package.json version field matches config.py APP_VERSION (both 1.5.0)"
    - "README.md states Python 3.12+ not Python 3.8+"
    - "installer/virelo.iss MyAppURL uses the GitHub URL matching APP_SUPPORT_URL in config.py"
  artifacts:
    - path: "frontend/package.json"
      provides: "Synchronized version string"
      contains: "\"version\": \"1.5.0\""
    - path: "README.md"
      provides: "Correct Python version requirement"
      contains: "Python 3.12+"
    - path: "installer/virelo.iss"
      provides: "GitHub support URL"
      contains: "https://github.com/yusufqwareeq/virelo"
  key_links:
    - from: "frontend/package.json version"
      to: "virelo/app/config.py APP_VERSION"
      via: "manual sync (enforced by CI version-check job added in Plan 02)"
      pattern: "\"version\": \"1.5.0\""
    - from: "installer/virelo.iss MyAppURL"
      to: "virelo/app/config.py APP_SUPPORT_URL"
      via: "#define MyAppURL"
      pattern: "github.com/yusufqwareeq/virelo"
---

<objective>
Synchronize three version/URL strings across the codebase so there is a single source of truth for each: (1) update frontend/package.json version from 1.4.2 to 1.5.0 matching APP_VERSION in config.py; (2) update README.md Python version requirement from 3.8+ to 3.12+ matching pyproject.toml; (3) update installer/virelo.iss MyAppURL from the mailto address to the GitHub URL matching APP_SUPPORT_URL in config.py.

Purpose: The CI version-check job added in Plan 02 will fail until package.json matches config.py. The other two are documentation correctness fixes that a fresh developer reads first.

Output: All three files updated with correct version/URL values.
</objective>

<execution_context>
@D:/projects/Virelo/.planning/phases/07-ci-and-repo-hygiene/07-CONTEXT.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/projects/Virelo/.planning/PROJECT.md
@D:/projects/Virelo/.planning/ROADMAP.md
@D:/projects/Virelo/.planning/STATE.md

<interfaces>
<!-- Source of truth values (read from codebase): -->
<!-- virelo/app/config.py line 4: APP_VERSION = "1.5.0" -->
<!-- virelo/app/config.py line 8: APP_SUPPORT_URL = "https://github.com/yusufqwareeq/virelo" -->
<!-- pyproject.toml: requires-python = ">=3.12" -->

<!-- Current state to replace: -->
<!-- frontend/package.json line 4: "version": "1.4.2" -->
<!-- README.md line 23: - Python 3.8+ -->
<!-- installer/virelo.iss line 3: #define MyAppURL "mailto:qwareeq8@gmail.com" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync frontend/package.json version and fix README Python version</name>
  <files>frontend/package.json, README.md</files>
  <read_first>
    - Read D:/projects/Virelo/frontend/package.json in full (25 lines — already confirmed in context; version is currently "1.4.2").
    - Read D:/projects/Virelo/README.md line 23 area to confirm "Python 3.8+" text location.
    - Read D:/projects/Virelo/virelo/app/config.py lines 1-10 to confirm APP_VERSION = "1.5.0".
    - Read D:/projects/Virelo/pyproject.toml to confirm requires-python = ">=3.12".
  </read_first>
  <action>
**File 1 — frontend/package.json (per D-09):**

Find line 4 which currently reads:
```json
  "version": "1.4.2",
```
Replace with:
```json
  "version": "1.5.0",
```
This is the only change to package.json. All other fields (name, type, scripts, dependencies, devDependencies) remain unchanged.

**File 2 — README.md (per D-11):**

Find the line that currently reads:
```markdown
- Python 3.8+
```
Replace with:
```markdown
- Python 3.12+
```
This is a single-line text substitution. All other README content is unchanged.
  </action>
  <verify>
    <automated>grep "\"version\": \"1.5.0\"" D:/projects/Virelo/frontend/package.json && grep "Python 3.12" D:/projects/Virelo/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"version": "1.5.0"' frontend/package.json` exits 0
    - `grep '"version": "1.4.2"' frontend/package.json` exits 1 (old version gone)
    - `grep "Python 3.12" README.md` exits 0
    - `grep "Python 3.8" README.md` exits 1 (old version gone)
    - package.json is valid JSON (no syntax errors introduced)
  </acceptance_criteria>
  <done>frontend/package.json version is "1.5.0". README.md states "Python 3.12+".</done>
</task>

<task type="auto">
  <name>Task 2: Update installer MyAppURL to GitHub URL</name>
  <files>installer/virelo.iss</files>
  <read_first>
    - Read D:/projects/Virelo/installer/virelo.iss lines 1-20 to confirm current MyAppURL value and that it has no #ifndef guard (unlike MyAppVersion which does).
    - Confirm line 3 reads: `#define MyAppURL "mailto:qwareeq8@gmail.com"`
    - Read D:/projects/Virelo/virelo/app/config.py line 8 to confirm APP_SUPPORT_URL = "https://github.com/yusufqwareeq/virelo"
    - Read CLAUDE.md footgun #4 to confirm why #ifndef is needed for MyAppVersion but NOT needed for MyAppURL (MyAppURL is never passed via /D flag).
  </read_first>
  <action>
Per D-12: Find line 3 in installer/virelo.iss which currently reads:
```iss
#define MyAppURL "mailto:qwareeq8@gmail.com"
```
Replace with:
```iss
#define MyAppURL "https://github.com/yusufqwareeq/virelo"
```

This is a direct `#define` edit. Unlike `MyAppVersion`, `MyAppURL` is not passed via the ISCC `/D` flag at build time (confirmed in CLAUDE.md footgun #4 — the `/D` override is only used for version). A plain `#define` replacement is correct and safe.

Do NOT add an `#ifndef` guard around `MyAppURL`.
  </action>
  <verify>
    <automated>grep "github.com/yusufqwareeq/virelo" D:/projects/Virelo/installer/virelo.iss && grep -c "mailto:" D:/projects/Virelo/installer/virelo.iss</automated>
  </verify>
  <acceptance_criteria>
    - `grep "github.com/yusufqwareeq/virelo" installer/virelo.iss` exits 0
    - `grep "mailto:" installer/virelo.iss` exits 1 (old URL gone)
    - `grep "MyAppURL" installer/virelo.iss` returns exactly 1 match containing the GitHub URL
    - No `#ifndef` wrapper around `#define MyAppURL` (it is a direct define)
  </acceptance_criteria>
  <done>installer/virelo.iss MyAppURL points to https://github.com/yusufqwareeq/virelo. No mailto address remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer URL | URL is hardcoded in source; displayed in Windows Add/Remove Programs — no injection risk since it is a static string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | virelo.iss MyAppURL | accept | Static GitHub URL; no external input; displayed read-only in Windows Add/Remove Programs |
| T-07-03-02 | Information Disclosure | README.md | accept | Public documentation; version number is not sensitive |
</threat_model>

<verification>
After both tasks:
1. `grep '"version": "1.5.0"' frontend/package.json` — exits 0
2. `grep "Python 3.12" README.md` — exits 0
3. `grep "github.com/yusufqwareeq/virelo" installer/virelo.iss` — exits 0
4. `grep "mailto:" installer/virelo.iss` — exits 1 (gone)
5. `grep '"version": "1.4.2"' frontend/package.json` — exits 1 (old value gone)
</verification>

<success_criteria>
- frontend/package.json "version" is "1.5.0" (matches config.py APP_VERSION)
- README.md Python version requirement says "3.12+" (matches pyproject.toml)
- installer/virelo.iss MyAppURL is "https://github.com/yusufqwareeq/virelo" (matches config.py APP_SUPPORT_URL)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ci-and-repo-hygiene/07-03-SUMMARY.md`
</output>
