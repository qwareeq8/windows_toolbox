---
phase: 07-ci-and-repo-hygiene
plan: 04
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - virelo/services/snap.py
  - virelo/app/window.py
  - tests/unit/test_snap_geometry.py
  - CLAUDE.md
autonomous: true
requirements:
  - CI-08

must_haves:
  truths:
    - "No source file in virelo/, tests/unit/, or CLAUDE.md contains the class names HotkeyListener or ShiftSnapRestore"
    - "virelo/services/snap.py exports MultiPressHotkeyListener and SnapRestoreController as the two class definitions"
    - "virelo/app/window.py imports and instantiates MultiPressHotkeyListener and SnapRestoreController"
    - "Unit tests still pass after the rename"
  artifacts:
    - path: "virelo/services/snap.py"
      provides: "Renamed class definitions"
      contains: "class MultiPressHotkeyListener"
    - path: "virelo/services/snap.py"
      provides: "Renamed class definitions"
      contains: "class SnapRestoreController"
    - path: "virelo/app/window.py"
      provides: "Updated import and instantiation"
      contains: "from virelo.services.snap import MultiPressHotkeyListener, SnapRestoreController, SnapService"
    - path: "tests/unit/test_snap_geometry.py"
      provides: "Updated test usage"
      contains: "SnapRestoreController.__new__(SnapRestoreController)"
    - path: "CLAUDE.md"
      provides: "Updated project structure docs"
      contains: "MultiPressHotkeyListener"
  key_links:
    - from: "virelo/app/window.py import"
      to: "virelo/services/snap.py class MultiPressHotkeyListener"
      via: "from virelo.services.snap import"
      pattern: "MultiPressHotkeyListener"
    - from: "virelo/app/window.py import"
      to: "virelo/services/snap.py class SnapRestoreController"
      via: "from virelo.services.snap import"
      pattern: "SnapRestoreController"
---

<objective>
Rename two stale class names throughout the codebase: HotkeyListener becomes MultiPressHotkeyListener (reflecting that keys are now configurable), and ShiftSnapRestore becomes SnapRestoreController (reflecting that the fixed shift-key assumption is gone). All occurrences in snap.py, window.py, test_snap_geometry.py, and CLAUDE.md are updated. Instance variable names (_hotkey_listener, shift_mgr) are intentionally NOT renamed per D-13/D-14.

Purpose: The class names were named for a now-removed assumption (Shift key hardcoded). The new names describe what the classes actually do. CI-08 requires these renames before phase is complete.

Output: Four files updated. No occurrence of HotkeyListener or ShiftSnapRestore remains in virelo/, tests/unit/, or CLAUDE.md. pytest tests/unit/ -q passes.
</objective>

<execution_context>
@D:/projects/Virelo/.planning/phases/07-ci-and-repo-hygiene/07-CONTEXT.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/projects/Virelo/.planning/PROJECT.md
@D:/projects/Virelo/.planning/ROADMAP.md
@D:/projects/Virelo/.planning/STATE.md

<interfaces>
<!-- All occurrence locations verified by grep against live codebase: -->

<!-- virelo/services/snap.py — HotkeyListener occurrences: -->
<!-- line 1: module docstring "HotkeyListener, and ShiftSnapRestore engine." -->
<!-- line 3: "HotkeyListener detects multi-press keyboard patterns..." -->
<!-- line 49: class HotkeyListener(QtCore.QObject): -->
<!-- line 338: """Set or replace the HotkeyListener instance.""" -->

<!-- virelo/services/snap.py — ShiftSnapRestore occurrences: -->
<!-- line 1: module docstring "...and ShiftSnapRestore engine." -->
<!-- line 4: "ShiftSnapRestore performs window snap and restore operations." -->
<!-- line 134: class ShiftSnapRestore(QtCore.QObject): -->
<!-- line 208: LOG.exception("ShiftSnapRestore.perform failed.", exc_info=e) -->
<!-- line 329: """Accept a ShiftSnapRestore instance (or None during early init).""" -->
<!-- line 334: """Set or replace the ShiftSnapRestore instance.""" -->

<!-- virelo/app/window.py — both names: -->
<!-- line 23: from virelo.services.snap import HotkeyListener, ShiftSnapRestore, SnapService -->
<!-- line 174: # snap_enabled used by business logic (ShiftSnapRestore, _test_snap) -->
<!-- line 197: # HotkeyListener + ShiftSnapRestore (per D-01/D-02/D-03) -->
<!-- line 198: self._hotkey_listener = HotkeyListener(self.settings)  [class name only; variable name unchanged] -->
<!-- line 199: self.shift_mgr = ShiftSnapRestore(self.settings)  [class name only; variable name unchanged] -->

<!-- tests/unit/test_snap_geometry.py — ShiftSnapRestore only (HotkeyListener not tested): -->
<!-- line 105: # -- ShiftSnapRestore restore -- -->
<!-- line 116: from virelo.services.snap import ShiftSnapRestore -->
<!-- line 118: # Create ShiftSnapRestore bypassing __init__... -->
<!-- line 119: mgr = ShiftSnapRestore.__new__(ShiftSnapRestore) -->

<!-- CLAUDE.md: -->
<!-- line 56: snap.py -- HotkeyListener (keyboard detection), ShiftSnapRestore (window movement), SnapService facade, geometry calculations -->

<!-- DO NOT rename in: .planning/ docs (D-16 — historical records) -->
<!-- DO NOT rename: instance variables _hotkey_listener, shift_mgr (D-13/D-14) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename classes in virelo/services/snap.py</name>
  <files>virelo/services/snap.py</files>
  <read_first>
    - Read D:/projects/Virelo/virelo/services/snap.py lines 1-10 (module docstring — has both old names).
    - Read D:/projects/Virelo/virelo/services/snap.py lines 45-55 (HotkeyListener class definition at line 49).
    - Read D:/projects/Virelo/virelo/services/snap.py lines 130-140 (ShiftSnapRestore class definition at line 134).
    - Read D:/projects/Virelo/virelo/services/snap.py lines 200-215 (LOG.exception at line 208).
    - Read D:/projects/Virelo/virelo/services/snap.py lines 325-345 (SnapService docstrings at lines 329, 334, 338).
  </read_first>
  <action>
Apply all HotkeyListener and ShiftSnapRestore renames in virelo/services/snap.py. Make each edit precisely — do not use a global search-replace that would catch unrelated code.

**HotkeyListener → MultiPressHotkeyListener (4 occurrences):**

1. Line 1 (module docstring first line):
   - Old: `"""Snap service, HotkeyListener, and ShiftSnapRestore engine.`
   - New: `"""Snap service, MultiPressHotkeyListener, and SnapRestoreController engine.`

2. Line 3 (module docstring body):
   - Old: `HotkeyListener detects multi-press keyboard patterns and emits a trigger signal.`
   - New: `MultiPressHotkeyListener detects multi-press keyboard patterns and emits a trigger signal.`

3. Line 49 (class definition):
   - Old: `class HotkeyListener(QtCore.QObject):`
   - New: `class MultiPressHotkeyListener(QtCore.QObject):`

4. Line 338 (SnapService.set_listener docstring):
   - Old: `"""Set or replace the HotkeyListener instance."""`
   - New: `"""Set or replace the MultiPressHotkeyListener instance."""`

**ShiftSnapRestore → SnapRestoreController (6 occurrences):**

5. Line 1 (already partially updated above — also contains "ShiftSnapRestore engine"):
   Already handled in item 1 above (both names appear on line 1).

6. Line 4 (module docstring body):
   - Old: `ShiftSnapRestore performs window snap and restore operations.`
   - New: `SnapRestoreController performs window snap and restore operations.`

7. Line 134 (class definition):
   - Old: `class ShiftSnapRestore(QtCore.QObject):`
   - New: `class SnapRestoreController(QtCore.QObject):`

8. Line 208 (LOG.exception message):
   - Old: `LOG.exception("ShiftSnapRestore.perform failed.", exc_info=e)`
   - New: `LOG.exception("SnapRestoreController.perform failed.", exc_info=e)`

9. Line 329 (SnapService.__init__ docstring):
   - Old: `"""Accept a ShiftSnapRestore instance (or None during early init)."""`
   - New: `"""Accept a SnapRestoreController instance (or None during early init)."""`

10. Line 334 (SnapService.set_manager docstring):
    - Old: `"""Set or replace the ShiftSnapRestore instance."""`
    - New: `"""Set or replace the SnapRestoreController instance."""`

After applying all edits, verify no occurrence of either old name remains in snap.py.
  </action>
  <verify>
    <automated>grep -n "ShiftSnapRestore\|HotkeyListener" D:/projects/Virelo/virelo/services/snap.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ShiftSnapRestore" virelo/services/snap.py` exits 1 (no matches)
    - `grep "HotkeyListener" virelo/services/snap.py` exits 1 (no matches)
    - `grep "class MultiPressHotkeyListener" virelo/services/snap.py` exits 0
    - `grep "class SnapRestoreController" virelo/services/snap.py` exits 0
    - `grep "SnapRestoreController.perform failed" virelo/services/snap.py` exits 0
    - `grep "MultiPressHotkeyListener instance" virelo/services/snap.py` exits 0
    - `grep "SnapRestoreController instance" virelo/services/snap.py` exits 0 (lines 329 and 334)
  </acceptance_criteria>
  <done>snap.py has zero occurrences of ShiftSnapRestore or HotkeyListener. Both new class names are present at their definition sites and in all docstrings/log messages.</done>
</task>

<task type="auto">
  <name>Task 2: Rename imports and usage in window.py, test_snap_geometry.py, and CLAUDE.md</name>
  <files>virelo/app/window.py, tests/unit/test_snap_geometry.py, CLAUDE.md</files>
  <read_first>
    - Read D:/projects/Virelo/virelo/app/window.py lines 20-30 (import line 23 area).
    - Read D:/projects/Virelo/virelo/app/window.py lines 170-205 (comment at 174, comment+instantiation at 197-199).
    - Read D:/projects/Virelo/tests/unit/test_snap_geometry.py lines 103-125 (section comment, function, import, instantiation).
    - Read D:/projects/Virelo/CLAUDE.md line 56 area (snap.py description line).
  </read_first>
  <action>
**File 1 — virelo/app/window.py (per D-13, D-14):**

Edit 1 — line 23 (import statement):
- Old: `from virelo.services.snap import HotkeyListener, ShiftSnapRestore, SnapService`
- New: `from virelo.services.snap import MultiPressHotkeyListener, SnapRestoreController, SnapService`

Edit 2 — line 174 (comment):
- Old: `# snap_enabled used by business logic (ShiftSnapRestore, _test_snap)`
- New: `# snap_enabled used by business logic (SnapRestoreController, _test_snap)`

Edit 3 — line 197 (comment):
- Old: `# HotkeyListener + ShiftSnapRestore (per D-01/D-02/D-03)`
- New: `# MultiPressHotkeyListener + SnapRestoreController (per D-01/D-02/D-03)`

Edit 4 — line 198 (instantiation — class name only, variable name _hotkey_listener unchanged):
- Old: `self._hotkey_listener = HotkeyListener(self.settings)`
- New: `self._hotkey_listener = MultiPressHotkeyListener(self.settings)`

Edit 5 — line 199 (instantiation — class name only, variable name shift_mgr unchanged):
- Old: `self.shift_mgr = ShiftSnapRestore(self.settings)`
- New: `self.shift_mgr = SnapRestoreController(self.settings)`

IMPORTANT: Do NOT rename the variable names `_hotkey_listener` or `shift_mgr` — only the class names change per D-13/D-14.

**File 2 — tests/unit/test_snap_geometry.py (per D-13):**

Edit 1 — line 105 (section comment):
- Old: `# -- ShiftSnapRestore restore --`
- New: `# -- SnapRestoreController restore --`

Edit 2 — line 116 (import inside test body):
- Old: `    from virelo.services.snap import ShiftSnapRestore`
- New: `    from virelo.services.snap import SnapRestoreController`

Edit 3 — line 118 (inline comment):
- Old: `    # Create ShiftSnapRestore bypassing __init__ (avoids keyboard hooks and EnumWindows)`
- New: `    # Create SnapRestoreController bypassing __init__ (avoids keyboard hooks and EnumWindows)`

Edit 4 — line 119 (instantiation):
- Old: `    mgr = ShiftSnapRestore.__new__(ShiftSnapRestore)`
- New: `    mgr = SnapRestoreController.__new__(SnapRestoreController)`

**File 3 — CLAUDE.md (per D-15):**

Edit 1 — line 56 (snap.py description in project structure):
- Old: `    - \`snap.py\` -- HotkeyListener (keyboard detection), ShiftSnapRestore (window movement), SnapService facade, geometry calculations`
- New: `    - \`snap.py\` -- MultiPressHotkeyListener (keyboard detection), SnapRestoreController (window movement), SnapService facade, geometry calculations`

After all edits, run pytest to verify no import errors were introduced.

IMPORTANT: Do NOT edit any files under .planning/ — per D-16, planning docs are historical records.
  </action>
  <verify>
    <automated>grep -rn "ShiftSnapRestore\|HotkeyListener" D:/projects/Virelo/virelo/ D:/projects/Virelo/tests/unit/ D:/projects/Virelo/CLAUDE.md 2>/dev/null && echo "FAIL: old names found" || echo "PASS: no old names in scope files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "ShiftSnapRestore" virelo/ tests/unit/ CLAUDE.md` exits 1 (no matches)
    - `grep -rn "HotkeyListener" virelo/ tests/unit/ CLAUDE.md` exits 1 (no matches)
    - `grep "from virelo.services.snap import MultiPressHotkeyListener, SnapRestoreController, SnapService" virelo/app/window.py` exits 0
    - `grep "self._hotkey_listener = MultiPressHotkeyListener" virelo/app/window.py` exits 0
    - `grep "self.shift_mgr = SnapRestoreController" virelo/app/window.py` exits 0
    - `grep "_hotkey_listener" virelo/app/window.py` still exits 0 (variable name preserved, not deleted)
    - `grep "shift_mgr" virelo/app/window.py` still exits 0 (variable name preserved, not deleted)
    - `grep "SnapRestoreController.__new__(SnapRestoreController)" tests/unit/test_snap_geometry.py` exits 0
    - `grep "MultiPressHotkeyListener" CLAUDE.md` exits 0
    - `pytest tests/unit/ -q` exits 0
    - `grep -rn "ShiftSnapRestore\|HotkeyListener" .planning/` — may have matches (planning docs are untouched per D-16; this is EXPECTED and NOT a failure)
  </acceptance_criteria>
  <done>window.py, test_snap_geometry.py, and CLAUDE.md contain no occurrence of ShiftSnapRestore or HotkeyListener. Instance variable names _hotkey_listener and shift_mgr are preserved. pytest tests/unit/ -q passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source | Class renames are source-only; no runtime API surface is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | Class rename (snap.py) | accept | Pure source rename; no change to behavior, signals, or runtime API |
| T-07-04-02 | Denial of Service | Import breakage if rename is incomplete | mitigate | Acceptance criteria includes pytest tests/unit/ -q passing; any missed rename would cause ImportError caught by tests |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "ShiftSnapRestore" virelo/ tests/unit/ CLAUDE.md` — exits 1 (zero matches)
2. `grep -rn "HotkeyListener" virelo/ tests/unit/ CLAUDE.md` — exits 1 (zero matches)
3. `grep "class MultiPressHotkeyListener" virelo/services/snap.py` — exits 0
4. `grep "class SnapRestoreController" virelo/services/snap.py` — exits 0
5. `pytest tests/unit/ -q` — exits 0 (all tests pass or are skipped)
6. Confirm .planning/ docs still contain the old names (D-16 compliance)
</verification>

<success_criteria>
- Zero occurrences of ShiftSnapRestore in virelo/, tests/unit/, CLAUDE.md
- Zero occurrences of HotkeyListener in virelo/, tests/unit/, CLAUDE.md
- MultiPressHotkeyListener and SnapRestoreController class definitions exist in snap.py
- window.py import updated; _hotkey_listener and shift_mgr variable names preserved
- pytest tests/unit/ -q exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-ci-and-repo-hygiene/07-04-SUMMARY.md`
</output>
